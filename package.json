{
  "name": "oid-reference",
  "version": "1.0.0",
  "description": "Paessler OID Reference — live lookup, search, and decode tool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "better-sqlite3": "^9.6.0",
    "node-html-parser": "^6.1.12"
  },
  "engines": {
    "node": ">=20"
  }
}
